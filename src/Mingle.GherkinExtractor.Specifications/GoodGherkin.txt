# http://mingle
@test1 @test2
Feature: My Test Feature

Background:

    Given I am signed in as an admin

Scenario: Admin required

    Given I am not signed in
    When I go to the filename_infos page
    Then I should see "Access denied. You must be an admin to access this page."

Scenario: Import a raw file

    Given I am on the filename_infos page
    When I follow "IMPORT" on the row containing "ZN__TEMP__VAL"
    Then I should see "Imported 5 readings"
    And I should see "Destroy" on the row containing "ZN__TEMP__VAL"